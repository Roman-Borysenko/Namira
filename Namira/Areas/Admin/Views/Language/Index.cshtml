@using Namira.Areas.Admin.ViewModels
@model List<Namira.Entities.Language>

@{ ViewBag.Title = "Language"; }

<div class="container">
    <h1>@ViewBag.Title</h1>
    <a asp-area="admin" asp-action="add" asp-controller="language" class="btn btn-outline-success">Add language</a>

    @await Html.PartialAsync(
        "../PartialViews/DataOutput",
        new DataOutputViewModel
            {
                Data = Model,
                DataType = typeof(Namira.Entities.Language),
                Fields = new string[] { "Id", "Name", "Reduction", "Slug"
            },
        EditLink = new Link() { Area = "Admin", Controller = "Language", Action = "Edit" },
        DeleteLink = new Link() { Area = "Admin", Controller = "Language", Action = "Delete" }
    })

    @await Html.PartialAsync("../PartialViews/Pagination", ViewBag.Paginator as Paginator)
</div>