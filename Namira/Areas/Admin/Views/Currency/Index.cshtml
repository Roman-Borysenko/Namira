@using Namira.Areas.Admin.ViewModels
@model List<Namira.Entities.Currency>

@{ ViewBag.Title = "Currency"; }

<div class="container">
    <h1>@ViewBag.Title</h1>
    <a asp-area="admin" asp-action="add" asp-controller="currency" class="btn btn-outline-success">Add currency</a>

    @await Html.PartialAsync(
        "../PartialViews/DataOutput",
        new DataOutputViewModel
            {
                Data = Model,
                DataType = typeof(Namira.Entities.Currency),
                Fields = new string[] { "Id", "Name", "Reduction", "Slug"
            },
        EditLink = new Link() { Area = "Admin", Controller = "Currency", Action = "Edit" },
        DeleteLink = new Link() { Area = "Admin", Controller = "Currency", Action = "Delete" }
    })

    @await Html.PartialAsync("../PartialViews/Pagination", ViewBag.Paginator as Paginator)
</div>