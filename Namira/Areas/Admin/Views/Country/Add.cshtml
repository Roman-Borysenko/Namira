@model Namira.Areas.Admin.Models.Country

@{
    ViewBag.Title = "Add country";
    var action = ViewBag.Page == "edit" ? "Edit" : "Add";
}

<div class="container">
    <h1>@ViewBag.Title</h1>
    <form asp-area="Admin" asp-action="@action" asp-controller="Country" method="post">
        <div class="validation" asp-validation-summary="ModelOnly"></div>
        <input asp-for="@Model.Id" type="hidden" />

        <div class="form-group">
            <label asp-for="@Model.Name">Name</label>
            <input asp-for="@Model.Name" type="text" class="form-control" id="name">
            <span asp-validation-for="@Model.Name" class="form-text text-muted"></span>
        </div>

        <div class="form-group">
            <button type="submit" class="btn btn-primary">Save</button>
        </div>
    </form>
</div>

@section scripts{
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}